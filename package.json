{
  "name": "alxmy.me",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i input.css -o output.css --watch",
    "build": "npx @tailwindcss/cli -i input.css -o dist/output.css --minify && cp-r images dist/images && cp -r fonts dist/fonts && cp index.html dist/index.html",
    "start": "npx live-server",
    "generate-og": "node generate-og.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "playwright": "^1.58.2",
    "tailwindcss": "^4.0.0"
  }
}
