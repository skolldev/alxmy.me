{
  "name": "alxmy.me",
  "private": true,
  "scripts": {
    "dev": "npx @tailwindcss/cli@^4 -i input.css -o output.css --watch",
    "build": "npx @tailwindcss/cli@^4 -i input.css -o output.css --minify",
    "start": "npx live-server"
  }
}
